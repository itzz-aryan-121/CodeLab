{
  "name": "collab-code-editor",
  "version": "1.0.0",
  "description": "* multiple users can join a room for simultaneous editing in realtime.\r * single user creates a room.\r * user who created/joined the room can share the room ID for invite.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "react-scripts build"

  },
  "author": "",
  "license": "ISC"
}
